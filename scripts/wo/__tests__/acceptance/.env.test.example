# Test Environment Configuration
# Copy this file to .env.test and fill in the values
#
# NOTE: Tests run in cache-only mode by default, using pre-recorded fixtures.
# No real API calls are made unless you run with NO_CACHE=true.

# Trello credentials (only needed when recording new fixtures with NO_CACHE=true)
TRELLO_API_KEY=<your-trello-api-key>
TRELLO_TOKEN=<your-trello-token>
TRELLO_BOARD_ID_WO=<test-board-id>

# GitHub Enterprise settings
GH_HOST=schibsted.ghe.com

# Test GitHub repo for creating ephemeral issues/PRs
GH_TEST_REPO=michal-matoga/utils
